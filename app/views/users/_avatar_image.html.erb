<% if user.avatar_image.present? %>
  <%= image_tag user.avatar_image.thumb(args).url, class: 'thumbnail' %>
<% else %>
  <img src="http://placehold.it/<%= args.gsub(/\W/, '') %>&text=Super+cool+avatar" alt="Super cool avatar" class="thumbnail" />
<% end %>